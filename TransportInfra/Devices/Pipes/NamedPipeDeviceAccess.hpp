#ifndef NAMEDPIPEDEVICEACESS_H
#define NAMEDPIPEDEVICEACESS_H
#include "Devices/GenericDeviceAccess.hpp"

namespace infra
{
class NamedPipeDeviceAccess 
{
    /*
    template<typename Host, typename FifoDevice, typename>
    friend class ConnectionPolicy;

    template<typename Host, typename FifoDevice, typename>
    friend class AcceptorPolicy;

    template<typename Host, typename FifoDevice, typename>
    friend class FifoIOPolicy;

    template<typename FifoDevice>
    static void close(FifoDevice & device) { device.close(); }

    template<typename Device>
    static decltype(auto) getHandle(const Device & device) { return device.getHandle(); }
    */
};

} //infra

#endif
